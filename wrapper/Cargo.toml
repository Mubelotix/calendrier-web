[package]
name = "calendrier-web"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
calendrier = { version="1.0", features = ["no-time-offset"] }

[profile.release]
lto = true
